  // fleet payment Model 
  /* 
  
  // 🔹 Billing Effective Date
  
  activatedAt: {
    type: Date,
    required: true,
  },

  // 🔹 Expiry Date (calculated)
  expiryDate: {
    type: Date,
    required: true,
    index: true,
  },

  gracePeriodDays: {
    type: Number,
    default: 3, 
  },

  status: {
    type: String,
    enum: ["ACTIVE", "EXPIRED", "CANCELLED"],
    default: "ACTIVE",
    index: true,
  },

  lastRenewedAt: Date,



   */